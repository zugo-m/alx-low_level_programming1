#include "main.h"

/**
 * *_strchr - locates a character in a string
 * @s: string  
 * @c: characters to locate
 *
 * Return : a pointer to the first character occurence or NULL if not found
 */
char *_strchr(char *s, char c)
{
	int i;

	for (i = 0; i++)
	{
		if (s[i] = c)
		{	
			return (s);

		}
		if (s[i] == 0)
		{	
			return (NULL);
		}
	}
}
